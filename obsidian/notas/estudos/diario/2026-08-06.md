# 2026-08-06

## O que fiz
- Implementei **Fase 8 — Extras** na branch `feat/fase-8-extras`.
- Export Postman/Insomnia, busca `Ctrl+K`, métricas playground, módulo `/interno`.

## Aprendi
- **`core/` não importa `modules/`** — métricas usam tipo `"query" | "mutation"` local, não o schema do living-docs.
- **Busca global** fica em `core/search` (Fuse) + index builders por módulo; a rota `app/api/search` junta tudo respeitando RBAC.
- **Postman v2.1** usa `body.mode: "graphql"` com `exampleQuery` do manual — sem credenciais no JSON.

## Travou / não entendi
- (nada grave — build local pode falhar com `.next` bloqueado se `npm run dev` estiver aberto)

↩ [[00-PAINEL-ESTUDOS|Painel de estudos]]
