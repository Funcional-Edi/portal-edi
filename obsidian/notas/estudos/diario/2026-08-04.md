# 2026-08-04

## O que fiz

- Integrei o Obsidian ao projeto: cofre em `obsidian/`, gerado a partir de `docs/` e `content/`
- Criei os scripts `obsidian:sync` (uma vez) e `obsidian:watch` (contínuo)
- Atualizei os checkboxes das Fases 2–6 em `docs/migracao/mapa-documentacao-funcional.md`
- Montei este painel de estudos
- Abri a branch `feat/ui-portal` e fundei a casca visual em `core/ui/`
  (`AppShell`, `Badge`, `SessionActions` + Geist + lucide)
- Migrei home, login, `ManualShell` e `AdminShell` para o header compartilhado
- Documentei em ADR-0010 e `docs/migracao/decisoes-ui.md`

## O que aprendi

- **Junction no Windows** — `obsidian/docs` não é cópia, é um atalho de pasta. Editar em
  um lugar reflete no outro porque é o **mesmo** arquivo no disco. Por isso está no
  `.gitignore`: o conteúdo real já é versionado em `docs/`
- **Geração a partir de fonte única** — as notas de `produtos/` saem de `manual.json`.
  Editar a nota gerada é inútil (o sync sobrescreve); a correção vai na origem
- **`watch` com debounce** — salvar um arquivo dispara vários eventos do sistema. O
  script espera 400 ms antes de sincronizar para não rodar 5 vezes seguidas
- **PowerShell não aceita `&&`** nesta versão. Usar `;` — descoberto na prática, ao ver
  o erro `O token '&&' não é um separador de instruções válido`
- **`npm run ci` para na primeira falha**, então a etapa que quebrou já indica o tipo do
  problema
- **Um Next por pasta** — `npm run dev` e `npm run build` disputam `.next/`; dois devs
  na mesma pasta também (porta + arquivos)
- **Shell em `core/ui`** — módulos não podem se importar; UI compartilhada não pode
  morar só em `living-docs-externa`
- **Login com aba explícita** — decidir SSO vs.dev pela senha preenchida era UX ruim;
  o método precisa ser escolhido na interface

## O que travou

- Rodei `git status && git log` e falhou. Causa: `&&` do bash não existe aqui.
  Solução: `git status; git log`
- `npm run ci na fase-3-admin` — o texto extra virou argumento do `next build`
  (`Invalid project directory ...\na`). Solução: só `npm run ci`
- Build com `EPERM` em `.next/trace` enquanto o `npm run dev` estava aberto.
  Solução: parar o dev antes do CI

## Não entendi ainda

- Como o `unstable_cache` do Next decide invalidar por tag (`services/cache-tags.ts`)
- Diferença prática entre `git pull origin main` e `git merge main` estando na branch

↩ [[diario|Diário]] · [[00-PAINEL-ESTUDOS|Painel de estudos]]
