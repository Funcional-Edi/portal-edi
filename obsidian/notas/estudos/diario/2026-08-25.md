# 2026-08-25

## O que fiz
- Implementei `/docs/api`: catálogo de schemas sincronizados + página de referência por produto (`/docs/api/[slug]`).
- **UX:** links cruzados manual ↔ referência, deep link `#mutation-createToken`, catálogo mostra pendentes de sync (ex.: wholesaler).
- Services `schema-reference.ts` e `get-published-schema.ts`; módulo `graphql-reference` no registry; middleware `/docs/:path*`.
- Fixtures `data/projects/im/schema.json` e `demo/schema.json` para dev/E2E.
- Testes: 7 unitários novos + E2E `docs-api-flow.spec.ts` (2 cenários, passou).

## Aprendi
- **Manual curado ≠ referência técnica** — manual usa `manual.json`; `/docs/api` lê snapshot de introspection em `data/projects/{slug}/schema.json`.
- Snapshot read-only no servidor evita introspection live no browser (mesma linha de segurança do playground).
- Link cruzado: campo no schema que também está no manual ganha "Ver no roteiro".

## Travou / não entendi
- Build/E2E falharam com `EPERM` em `.next/trace` — dev server na porta 3002 bloqueando. Solução: matar processo + limpar `.next`.

↩ [[00-PAINEL-ESTUDOS|Painel de estudos]]
