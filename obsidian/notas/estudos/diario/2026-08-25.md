# 2026-08-25

## O que fiz
- Implementei `/docs/api` + UX links cruzados e catálogo pendentes.
- **Painel:** nova aba **Comandos** em `docs/painel/index.html` — Git, branches, PowerShell, Cursor, npm (busca integrada).

## Aprendi
- **Manual curado ≠ referência técnica** — manual usa `manual.json`; `/docs/api` lê snapshot de introspection em `data/projects/{slug}/schema.json`.
- Snapshot read-only no servidor evita introspection live no browser (mesma linha de segurança do playground).
- Link cruzado: campo no schema que também está no manual ganha "Ver no roteiro".

## Travou / não entendi
- Build/E2E falharam com `EPERM` em `.next/trace` — dev server na porta 3002 bloqueando. Solução: matar processo + limpar `.next`.

↩ [[00-PAINEL-ESTUDOS|Painel de estudos]]
