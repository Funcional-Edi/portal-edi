# 2026-08-10

## O que fiz
- Implementei a Fase 10 polish: export PDF (`/api/living-docs/projects/[slug]/export/pdf`), E2E de allowlist do playground e validação de projetos publicados.
- Atualizei docs de migração e marquei `canal-autorizador` como **seed-only** (sem conteúdo real legado ainda).

## Aprendi
- Dá para gerar PDF leve no servidor sem biblioteca pesada, escrevendo estrutura PDF básica (`%PDF`, objetos, `xref`) em memória.
- Regra operacional útil: projeto publicado precisa de `graphqlUrl` + pelo menos 1 operação para não quebrar export/playground.

## Travou / não entendi
- Teste E2E de operação allowlisted pode depender do gateway real; por isso o smoke automatizado valida "não bloqueou na allowlist" e o cenário SSO/homolog completo fica manual.

## Não entendi ainda
- Quando compensa trocar o PDF leve por render HTML->PDF com estilo rico (trade-off de peso vs. fidelidade visual).

↩ [[00-PAINEL-ESTUDOS|Painel de estudos]]
