# 2026-08-07

## O que fiz
- **Fase 9 — Consolidação homolog:** RBAC em `data/permissions.json`, smoke script, Dockerfile, docs deploy/CMS.

## Aprendi
- **`getPermissionsConfig()`** com cache em memória — auth lê arquivo uma vez por processo.
- **`output: standalone`** no Next.js simplifica Docker (só copia `.next/standalone` + `content/`).
- Smoke automatizado **não substitui** SSO manual — separa checks locais de passos com rede.

## Travou / não entendi
- (nada)

↩ [[00-PAINEL-ESTUDOS|Painel de estudos]]
