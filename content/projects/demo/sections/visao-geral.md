# Visão geral

Este manual demo mostra o fluxo mínimo de integração de inventário (IM):

1. Obter um token no gateway (`createToken`)
2. Enviar a carga de estoque (`saveInventories`)

Use o roteiro abaixo na ordem. Em homologação, o gateway é
`gateway-homologa.fidelize.com.br`.
